{
  "canonical_rtl": [
    "rtl/ace2_softmax_core.sv"
  ],
  "classification": "standalone_core",
  "dependencies": {
    "shared_paths": [
      "rtl/ace2_shell.sv",
      "verification/tb/ace2_shell_tb.sv"
    ]
  },
  "interfaces": [
    "start ready/valid",
    "context count",
    "packed score vector input",
    "packed probability vector output ready/valid",
    "saturation status"
  ],
  "known_limitations": [
    "The ACE-2 shell instantiates CONTEXT_MAX=8.",
    "The LUT/rounding behavior is fixed-point ACE-2 behavior, not a general floating-point softmax implementation."
  ],
  "license": {
    "inheritance": "Apache-2.0",
    "source": "LICENSE"
  },
  "maturity": "verified RTL core integrated and focused through ace2_shell",
  "name": "softmax",
  "summary": "Reusable fixed-point bounded-context softmax with Q15 probability outputs.",
  "supported_parameters": {
    "CONTEXT_MAX_default": 8,
    "PROB_WIDTH": 16,
    "SCORE_WIDTH": 16,
    "probability_format": "Q15"
  },
  "title": "Softmax Core",
  "verification": {
    "command": "make ip-demo IP=softmax",
    "operators": [
      "softmax"
    ],
    "proof_kind": "focused shared shell proof of standalone core",
    "proof_note": "Runs the +SOFTMAX_ONLY shell mode against the canonical softmax core.",
    "slow": false
  }
}
